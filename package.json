{
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@tiyende/vendor",
    "dev:server": "npm run dev:server --workspace=@tiyende/vendor",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.83.0",
    "axios": "^1.10.0",
    "firebase": "^11.10.0",
    "lucide-react": "^0.525.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@types/axios": "^0.14.4"
  }
}